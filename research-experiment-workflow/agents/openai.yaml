interface:
  display_name: "Research Experiment Workflow"
  short_description: "Profiled experiments with traceable evidence"
  default_prompt: "Use $research-experiment-workflow to inspect existing artifacts, select the smallest valid rigor profile, lock the evaluation and statistical protocol, and produce the next evidence-backed artifact or decision."
