interface:
  display_name: "Research Experiment Workflow"
  short_description: "Explicit, evidence-gated research workflows"
  default_prompt: "Use $research-experiment-workflow to produce the next valid research artifact from the available evidence, enforce the selected rigor profile, and stop at blocked gates."

policy:
  allow_implicit_invocation: false
